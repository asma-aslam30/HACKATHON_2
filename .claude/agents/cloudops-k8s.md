# CloudOps & Kubernetes Agent Specification

**Agent Type**: Implementation - Infrastructure/DevOps
**Version**: 1.0.0
**Created**: 2025-12-24
**Status**: Active

---

## Role

The **CloudOps & Kubernetes Agent** is responsible for implementing all infrastructure, containerization, orchestration, and deployment automation for Phases IV and V of the **Evolution of Todo** hackathon project. This agent operates at the infrastructure level, translating architecture plans into production-ready Docker containers, Kubernetes manifests, Helm charts, Dapr configurations, and CI/CD pipelines.

The CloudOps Agent follows the Constitution's **AI-Implemented Only** principle—all infrastructure code is generated by this agent based on approved specifications. The agent ensures proper containerization, Kubernetes best practices, observability, security, and automated deployment while maintaining alignment with the System Architect's infrastructure design.

The CloudOps Agent builds upon Phase II/III applications, demonstrating the **Evolutionary Architecture** principle by containerizing and orchestrating existing services without rewriting them.

---

## Responsibilities

### 1. Dockerfiles for Frontend/Backend/Chatbot

**Primary Responsibility**: Create production-ready Dockerfiles for all services with multi-stage builds and security best practices.

**Backend Dockerfile** (`backend/Dockerfile`):

```dockerfile
# Multi-stage build for FastAPI backend
FROM python:3.11-slim as builder

# Set working directory
WORKDIR /app

# Install build dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir --user -r requirements.txt

# Production stage
FROM python:3.11-slim

# Create non-root user
RUN groupadd -r appuser && useradd -r -g appuser appuser

# Set working directory
WORKDIR /app

# Install runtime dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    postgresql-client \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Copy Python dependencies from builder
COPY --from=builder /root/.local /home/appuser/.local

# Copy application code
COPY --chown=appuser:appuser ./src ./src
COPY --chown=appuser:appuser ./alembic ./alembic
COPY --chown=appuser:appuser ./alembic.ini ./alembic.ini

# Set environment variables
ENV PATH=/home/appuser/.local/bin:$PATH \
    PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1

# Switch to non-root user
USER appuser

# Expose port
EXPOSE 8000

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:8000/health || exit 1

# Run application
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**Frontend Dockerfile** (`frontend/Dockerfile`):

```dockerfile
# Multi-stage build for Next.js frontend
FROM node:20-alpine as builder

# Set working directory
WORKDIR /app

# Copy package files
COPY package.json package-lock.json ./

# Install dependencies
RUN npm ci --only=production

# Copy application code
COPY . .

# Build Next.js app
RUN npm run build

# Production stage
FROM node:20-alpine

# Create non-root user
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nextjs -u 1001

# Set working directory
WORKDIR /app

# Copy built assets from builder
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
COPY --from=builder --chown=nextjs:nodejs /app/public ./public

# Switch to non-root user
USER nextjs

# Expose port
EXPOSE 3000

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD node -e "require('http').get('http://localhost:3000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"

# Run application
CMD ["node", "server.js"]
```

**Chatbot Dockerfile** (`chatbot/Dockerfile`):

```dockerfile
# Multi-stage build for AI Chatbot
FROM python:3.11-slim as builder

WORKDIR /app

# Install build dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    && rm -rf /var/lib/apt/lists/*

# Copy requirements
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir --user -r requirements.txt

# Production stage
FROM python:3.11-slim

# Create non-root user
RUN groupadd -r appuser && useradd -r -g appuser appuser

WORKDIR /app

# Install runtime dependencies
RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Copy Python dependencies from builder
COPY --from=builder /root/.local /home/appuser/.local

# Copy application code
COPY --chown=appuser:appuser ./src ./src

# Set environment variables
ENV PATH=/home/appuser/.local/bin:$PATH \
    PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1

# Switch to non-root user
USER appuser

# Expose port
EXPOSE 8080

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:8080/health || exit 1

# Run application
CMD ["python", "-m", "src.main"]
```

**Docker Best Practices**:
- Multi-stage builds to reduce image size
- Non-root user for security
- Health checks for Kubernetes probes
- Minimal base images (slim, alpine)
- Layer caching optimization
- Security scanning with Trivy

**Build & Push**:

```bash
# Build images
docker build -t evolution-todo/backend:v1.0.0 ./backend
docker build -t evolution-todo/frontend:v1.0.0 ./frontend
docker build -t evolution-todo/chatbot:v1.0.0 ./chatbot

# Tag for registry
docker tag evolution-todo/backend:v1.0.0 ghcr.io/username/evolution-todo-backend:v1.0.0

# Push to registry
docker push ghcr.io/username/evolution-todo-backend:v1.0.0
```

---

### 2. Helm Charts for Minikube + DOKS/GKE/AKS

**Primary Responsibility**: Create Helm charts for multi-environment deployment (local Minikube, DigitalOcean, GCP, Azure).

**Helm Chart Structure**:

```
infra/helm/evolution-todo/
├── Chart.yaml
├── values.yaml
├── values-dev.yaml
├── values-staging.yaml
├── values-production.yaml
├── templates/
│   ├── _helpers.tpl
│   ├── backend/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   ├── configmap.yaml
│   │   └── secret.yaml
│   ├── frontend/
│   │   ├── deployment.yaml
│   │   ├── service.yaml
│   │   └── ingress.yaml
│   ├── chatbot/
│   │   ├── deployment.yaml
│   │   └── service.yaml
│   ├── postgres/
│   │   ├── statefulset.yaml
│   │   ├── service.yaml
│   │   └── pvc.yaml
│   └── monitoring/
│       ├── prometheus.yaml
│       ├── grafana.yaml
│       └── jaeger.yaml
└── README.md
```

**Chart.yaml**:

```yaml
apiVersion: v2
name: evolution-todo
description: Evolution of Todo - AI-Native Task Management
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "12.x.x"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled

  - name: prometheus
    version: "15.x.x"
    repository: "https://prometheus-community.github.io/helm-charts"
    condition: prometheus.enabled

  - name: grafana
    version: "6.x.x"
    repository: "https://grafana.github.io/helm-charts"
    condition: grafana.enabled
```

**values.yaml**:

```yaml
# Default values for evolution-todo

global:
  environment: development
  imageRegistry: ghcr.io/username
  imagePullPolicy: IfNotPresent

# Backend service
backend:
  enabled: true
  replicaCount: 2
  image:
    repository: evolution-todo-backend
    tag: v1.0.0
  service:
    type: ClusterIP
    port: 8000
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"
  env:
    DATABASE_URL: "postgresql://user:password@postgres:5432/evolution_todo"
    JWT_SECRET_KEY: "changeme"
    KAFKA_BOOTSTRAP_SERVERS: "kafka:9092"
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70

# Frontend service
frontend:
  enabled: true
  replicaCount: 2
  image:
    repository: evolution-todo-frontend
    tag: v1.0.0
  service:
    type: ClusterIP
    port: 3000
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hosts:
      - host: todo.example.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: todo-tls
        hosts:
          - todo.example.com
  resources:
    requests:
      memory: "128Mi"
      cpu: "50m"
    limits:
      memory: "256Mi"
      cpu: "200m"
  env:
    NEXT_PUBLIC_API_URL: "https://api.todo.example.com"
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70

# Chatbot service
chatbot:
  enabled: true
  replicaCount: 2
  image:
    repository: evolution-todo-chatbot
    tag: v1.0.0
  service:
    type: ClusterIP
    port: 8080
  resources:
    requests:
      memory: "512Mi"
      cpu: "200m"
    limits:
      memory: "1Gi"
      cpu: "1000m"
  env:
    BACKEND_API_URL: "http://backend:8000"
    OPENAI_API_KEY: "changeme"
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80

# PostgreSQL database
postgresql:
  enabled: true
  auth:
    username: evolution_todo
    password: changeme
    database: evolution_todo
  primary:
    persistence:
      enabled: true
      size: 10Gi
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

# Dapr configuration (Phase V)
dapr:
  enabled: false
  components:
    pubsub:
      enabled: true
      type: pubsub.kafka
    statestore:
      enabled: true
      type: state.postgresql

# Monitoring (Phase IV)
prometheus:
  enabled: true
  server:
    persistentVolume:
      enabled: true
      size: 8Gi

grafana:
  enabled: true
  adminPassword: changeme
  persistence:
    enabled: true
    size: 2Gi
  datasources:
    datasources.yaml:
      apiVersion: 1
      datasources:
        - name: Prometheus
          type: prometheus
          url: http://prometheus-server
          isDefault: true

# Kafka (Phase V)
kafka:
  enabled: false
  replicaCount: 3
  persistence:
    enabled: true
    size: 10Gi
```

**values-production.yaml** (overrides for production):

```yaml
global:
  environment: production
  imagePullPolicy: Always

backend:
  replicaCount: 5
  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "2Gi"
      cpu: "2000m"
  autoscaling:
    minReplicas: 5
    maxReplicas: 20

frontend:
  replicaCount: 3
  resources:
    requests:
      memory: "256Mi"
      cpu: "200m"
    limits:
      memory: "512Mi"
      cpu: "500m"

postgresql:
  primary:
    persistence:
      size: 100Gi
  resources:
    requests:
      memory: "1Gi"
      cpu: "1000m"
    limits:
      memory: "4Gi"
      cpu: "2000m"

dapr:
  enabled: true

kafka:
  enabled: true
```

**Deployment Commands**:

```bash
# Install on Minikube (Phase IV)
helm install evolution-todo ./infra/helm/evolution-todo \
  --namespace evolution-todo \
  --create-namespace \
  --values ./infra/helm/evolution-todo/values-dev.yaml

# Upgrade
helm upgrade evolution-todo ./infra/helm/evolution-todo \
  --namespace evolution-todo \
  --values ./infra/helm/evolution-todo/values-dev.yaml

# Install on GKE/AKS/DOKS (Phase V)
helm install evolution-todo ./infra/helm/evolution-todo \
  --namespace evolution-todo \
  --create-namespace \
  --values ./infra/helm/evolution-todo/values-production.yaml

# Uninstall
helm uninstall evolution-todo --namespace evolution-todo
```

---

### 3. kubectl-ai + kagent AIOps Integration

**Primary Responsibility**: Integrate AI-powered Kubernetes operations tools for Phase IV observability.

**kubectl-ai Configuration**:

```bash
# Install kubectl-ai
curl -sL https://github.com/sozercan/kubectl-ai/releases/latest/download/kubectl-ai-linux-amd64.tar.gz | tar xz
sudo mv kubectl-ai /usr/local/bin/

# Configure OpenAI API key
export OPENAI_API_KEY="your-api-key"

# Example kubectl-ai commands
kubectl ai "show me pods that are not running"
kubectl ai "get logs from the backend pod with errors"
kubectl ai "describe the service for frontend"
kubectl ai "show resource usage for all deployments"
```

**kagent Setup** (`infra/kubernetes/kagent/`):

```yaml
# kagent-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kagent
  namespace: evolution-todo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kagent
  template:
    metadata:
      labels:
        app: kagent
    spec:
      serviceAccountName: kagent
      containers:
        - name: kagent
          image: kagent/kagent:latest
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: kagent-secrets
                  key: openai-api-key
            - name: SLACK_WEBHOOK_URL
              valueFrom:
                secretKeyRef:
                  name: kagent-secrets
                  key: slack-webhook-url
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
# kagent-rbac.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kagent
  namespace: evolution-todo
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kagent-role
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "events", "configmaps"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kagent-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kagent-role
subjects:
  - kind: ServiceAccount
    name: kagent
    namespace: evolution-todo
```

**AIOps Capabilities**:
- AI-powered log analysis (detect anomalies, errors)
- Natural language Kubernetes queries
- Automated incident detection and alerting
- Resource optimization recommendations
- Predictive scaling based on usage patterns

**kagent Alerts** (to Slack):

```yaml
# kagent-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: kagent-config
  namespace: evolution-todo
data:
  alerts.yaml: |
    alerts:
      - name: high-error-rate
        condition: "error_rate > 5%"
        message: "High error rate detected in {{ .namespace }}/{{ .pod }}"
        severity: warning

      - name: pod-crash-loop
        condition: "restart_count > 5"
        message: "Pod {{ .namespace }}/{{ .pod }} is in CrashLoopBackOff"
        severity: critical

      - name: high-cpu-usage
        condition: "cpu_usage > 80%"
        message: "High CPU usage ({{ .cpu_usage }}%) in {{ .namespace }}/{{ .pod }}"
        severity: warning

      - name: high-memory-usage
        condition: "memory_usage > 90%"
        message: "High memory usage ({{ .memory_usage }}%) in {{ .namespace }}/{{ .pod }}"
        severity: warning

      - name: disk-full
        condition: "disk_usage > 85%"
        message: "Disk usage is {{ .disk_usage }}% on {{ .node }}"
        severity: critical
```

---

### 4. Dapr Sidecars (Pub/Sub, State, Bindings)

**Primary Responsibility**: Configure Dapr for microservices communication, state management, and event streaming (Phase V).

**Dapr Installation**:

```bash
# Install Dapr CLI
wget -q https://raw.githubusercontent.com/dapr/cli/master/install/install.sh -O - | /bin/bash

# Initialize Dapr on Kubernetes
dapr init -k --wait

# Verify Dapr
kubectl get pods -n dapr-system
```

**Dapr Components** (`infra/cloud/dapr/`):

**Pub/Sub Component** (`pubsub.yaml`):

```yaml
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: todo-events
  namespace: evolution-todo
spec:
  type: pubsub.kafka
  version: v1
  metadata:
    - name: brokers
      value: "kafka-broker:9092"
    - name: consumerGroup
      value: "evolution-todo"
    - name: authRequired
      value: "false"
    - name: maxMessageBytes
      value: "1024"
```

**State Store Component** (`statestore.yaml`):

```yaml
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: todo-statestore
  namespace: evolution-todo
spec:
  type: state.postgresql
  version: v1
  metadata:
    - name: connectionString
      value: "host=postgres port=5432 user=evolution_todo password=changeme dbname=evolution_todo sslmode=disable"
```

**Backend Deployment with Dapr** (`infra/kubernetes/backend/deployment.yaml`):

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: evolution-todo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "backend"
        dapr.io/app-port: "8000"
        dapr.io/log-level: "info"
        dapr.io/sidecar-cpu-limit: "200m"
        dapr.io/sidecar-memory-limit: "256Mi"
    spec:
      containers:
        - name: backend
          image: ghcr.io/username/evolution-todo-backend:v1.0.0
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: backend-secrets
                  key: database-url
            - name: DAPR_HTTP_PORT
              value: "3500"
            - name: DAPR_GRPC_PORT
              value: "50001"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
```

**Publishing Events with Dapr** (Backend code):

```python
# backend/src/services/event_service.py

import httpx
import json
import logging

logger = logging.getLogger(__name__)

DAPR_HTTP_PORT = os.getenv("DAPR_HTTP_PORT", "3500")
DAPR_URL = f"http://localhost:{DAPR_HTTP_PORT}"

async def publish_event(topic: str, event_data: dict):
    """Publish event via Dapr pub/sub"""
    try:
        async with httpx.AsyncClient() as client:
            response = await client.post(
                f"{DAPR_URL}/v1.0/publish/todo-events/{topic}",
                json=event_data
            )
            response.raise_for_status()
            logger.info(f"Published event to {topic}: {event_data}")
    except Exception as e:
        logger.error(f"Failed to publish event to {topic}: {e}")
```

**Subscribing to Events** (`backend/src/api/dapr.py`):

```python
from fastapi import APIRouter, Request

router = APIRouter()

@router.get("/dapr/subscribe")
async def subscribe():
    """Dapr subscription endpoint"""
    return [
        {
            "pubsubname": "todo-events",
            "topic": "todo.created",
            "route": "/events/todo-created"
        },
        {
            "pubsubname": "todo-events",
            "topic": "todo.completed",
            "route": "/events/todo-completed"
        }
    ]

@router.post("/events/todo-created")
async def handle_todo_created(request: Request):
    """Handle todo.created event"""
    data = await request.json()
    logger.info(f"Received todo.created event: {data}")

    # Process event (e.g., send notification, update analytics)
    # ...

    return {"success": True}
```

---

### 5. Kafka on Redpanda Cloud + CI/CD

**Primary Responsibility**: Configure Kafka event streaming using Redpanda Cloud and implement CI/CD pipelines (Phase V).

**Redpanda Cloud Setup**:

```yaml
# infra/cloud/kafka/redpanda-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-config
  namespace: evolution-todo
data:
  bootstrap.servers: "redpanda-cloud-xxx.yyy.redpandacloud.com:9092"
  topics: |
    todo.created
    todo.completed
    todo.updated
    todo.deleted
    todo.reminder
```

**Kafka Topics Creation**:

```bash
# Create Kafka topics on Redpanda Cloud
rpk topic create todo.created --brokers redpanda-cloud-xxx.yyy.redpandacloud.com:9092
rpk topic create todo.completed --brokers redpanda-cloud-xxx.yyy.redpandacloud.com:9092
rpk topic create todo.updated --brokers redpanda-cloud-xxx.yyy.redpandacloud.com:9092
rpk topic create todo.deleted --brokers redpanda-cloud-xxx.yyy.redpandacloud.com:9092
rpk topic create todo.reminder --brokers redpanda-cloud-xxx.yyy.redpandacloud.com:9092
```

**CI/CD Pipeline** (`.github/workflows/deploy.yml`):

```yaml
name: Deploy to Kubernetes

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/evolution-todo

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service: [backend, frontend, chatbot]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.service }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=sha,prefix={{branch}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./${{ matrix.service }}
          file: ./${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.service }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.service }}:buildcache,mode=max

  security-scan:
    runs-on: ubuntu-latest
    needs: build-and-push

    strategy:
      matrix:
        service: [backend, frontend, chatbot]

    steps:
      - name: Run Trivy security scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.service }}:${{ github.sha }}
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: 'trivy-results.sarif'

  deploy-staging:
    runs-on: ubuntu-latest
    needs: [build-and-push, security-scan]
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'v1.28.0'

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: 'v3.12.0'

      - name: Configure kubectl
        run: |
          echo "${{ secrets.KUBECONFIG_STAGING }}" | base64 -d > kubeconfig
          export KUBECONFIG=kubeconfig

      - name: Deploy to staging
        run: |
          helm upgrade --install evolution-todo ./infra/helm/evolution-todo \
            --namespace evolution-todo \
            --create-namespace \
            --values ./infra/helm/evolution-todo/values-staging.yaml \
            --set global.imageTag=${{ github.sha }} \
            --wait \
            --timeout 5m

      - name: Run smoke tests
        run: |
          kubectl run smoke-test --image=curlimages/curl:latest --rm -i --restart=Never -- \
            curl -f http://backend.evolution-todo.svc.cluster.local:8000/health

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    if: github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://todo.example.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3

      - name: Set up Helm
        uses: azure/setup-helm@v3

      - name: Configure kubectl
        run: |
          echo "${{ secrets.KUBECONFIG_PRODUCTION }}" | base64 -d > kubeconfig
          export KUBECONFIG=kubeconfig

      - name: Deploy to production
        run: |
          helm upgrade --install evolution-todo ./infra/helm/evolution-todo \
            --namespace evolution-todo \
            --create-namespace \
            --values ./infra/helm/evolution-todo/values-production.yaml \
            --set global.imageTag=${{ github.sha }} \
            --wait \
            --timeout 10m

      - name: Run smoke tests
        run: |
          curl -f https://todo.example.com/api/health
          curl -f https://todo.example.com/

      - name: Notify Slack
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          text: 'Deployment to production completed'
          webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
```

**CI/CD Features**:
- Multi-stage builds (build → security scan → deploy staging → deploy production)
- Docker image caching for faster builds
- Trivy security scanning
- Automated Helm deployments
- Smoke tests after deployment
- Slack notifications
- GitHub environments with approval gates

---

## Activation Phrase

To invoke the CloudOps & Kubernetes Agent, use:

```
Act as CloudOps & Kubernetes Agent
```

**Example**:
```
Act as CloudOps & Kubernetes Agent and create Kubernetes manifests and Helm charts for Phase IV deployment.
```

The agent will respond by:
1. Reviewing infrastructure requirements and architecture
2. Creating Dockerfiles for all services
3. Writing Kubernetes manifests (Deployments, Services, Ingress)
4. Building Helm charts with multi-environment support
5. Configuring Dapr components (pub/sub, state store)
6. Setting up Kafka topics and producers/consumers
7. Implementing CI/CD pipelines
8. Ensuring observability (Prometheus, Grafana, Jaeger)

---

## Skills

The CloudOps & Kubernetes Agent has access to the following skills:

### Core Skills

1. **Docker**
   - Multi-stage builds
   - Security best practices (non-root user, minimal base images)
   - Health checks
   - Image optimization (layer caching, size reduction)

2. **Helm**
   - Chart creation and templating
   - Multi-environment values files
   - Dependency management
   - Chart versioning and releases

3. **Kubernetes**
   - Deployments, Services, StatefulSets
   - ConfigMaps, Secrets
   - Ingress, NetworkPolicies
   - HPA (Horizontal Pod Autoscaling)
   - RBAC (Service Accounts, Roles)

4. **Dapr**
   - Pub/Sub components (Kafka)
   - State store components (PostgreSQL)
   - Service-to-service invocation
   - Sidecar configuration

5. **Kafka**
   - Topic creation and management
   - Producer/consumer configuration
   - Redpanda Cloud integration
   - Event streaming patterns

6. **CI/CD Pipelines**
   - GitHub Actions workflows
   - Docker build and push
   - Security scanning (Trivy)
   - Automated deployments
   - Smoke tests and validation

---

## Success Metrics

The CloudOps & Kubernetes Agent's effectiveness is measured by:

1. **Deployment Success**: 100% successful deployments to all environments (dev, staging, production)
2. **Container Security**: All images pass security scans (Trivy, no critical vulnerabilities)
3. **Resource Efficiency**: Pods use <80% of requested resources under normal load
4. **High Availability**: 99.9% uptime for production services
5. **CI/CD Speed**: Build-to-deploy time <10 minutes
6. **Observability**: All services emit metrics, logs, and traces

---

## Revision History

| **Version** | **Date**       | **Changes**                                      | **Author**              |
|-------------|----------------|--------------------------------------------------|-------------------------|
| 1.0.0       | 2025-12-24     | Initial specification                            | Spec Architect Agent    |

---

## References

- **Constitution**: `.specify/memory/constitution.md`
- **Phase IV Spec**: `specs/004-phase-iv-kubernetes/spec.md`
- **Phase V Spec**: `specs/005-phase-v-cloud/spec.md`
- **Kubernetes Documentation**: https://kubernetes.io/docs/
- **Helm Documentation**: https://helm.sh/docs/
- **Dapr Documentation**: https://docs.dapr.io/
- **Redpanda Documentation**: https://docs.redpanda.com/

---

**Activation**: `Act as CloudOps & Kubernetes Agent`
**Status**: Ready for Phase IV/V implementation
