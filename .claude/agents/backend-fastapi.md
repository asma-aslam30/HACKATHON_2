# Backend / FastAPI Pro Agent Specification

**Agent Type**: Implementation - Backend
**Version**: 1.0.0
**Created**: 2025-12-24
**Status**: Active

---

## Role

The **Backend / FastAPI Pro Agent** is responsible for implementing all backend services across Phases II through V of the **Evolution of Todo** hackathon project. This agent operates at the implementation level, translating specifications and architectural plans into production-ready Python code using FastAPI, SQLModel, and related technologies.

The Backend Agent follows the Constitution's **AI-Implemented Only** principle—all backend code is generated by this agent based on approved specifications. The agent ensures code quality, security, testability, and observability while maintaining alignment with the System Architect's design decisions.

The Backend Agent does not make architectural decisions or write specifications; it focuses purely on implementing the backend services according to the specs and plans provided by the Spec Architect and System Architect agents.

---

## Responsibilities

### 1. FastAPI Routes and API Implementation (Phase II+)

**Primary Responsibility**: Implement all REST API endpoints defined in the API specifications.

**API Route Structure**:

```python
# backend/src/main.py
from fastapi import FastAPI, Depends, HTTPException, status
from fastapi.middleware.cors import CORSMiddleware
from sqlmodel import Session
from .database import get_session
from .api import auth, todos
from .middleware import auth_middleware

app = FastAPI(
    title="Evolution of Todo API",
    version="1.0.0",
    docs_url="/api/docs",
    redoc_url="/api/redoc"
)

# CORS middleware for frontend
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "https://evolution-todo.vercel.app"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Health check endpoint
@app.get("/health")
async def health_check():
    return {"status": "healthy", "version": "1.0.0"}

# API routers
app.include_router(auth.router, prefix="/api/auth", tags=["Authentication"])
app.include_router(todos.router, prefix="/api/todos", tags=["Todos"])
```

**Authentication Routes** (`backend/src/api/auth.py`):

```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlmodel import Session, select
from ..database import get_session
from ..models import User, UserCreate, UserLogin, Token
from ..services.auth_service import (
    hash_password,
    verify_password,
    create_access_token,
    get_current_user
)

router = APIRouter()

@router.post("/register", response_model=Token, status_code=status.HTTP_201_CREATED)
async def register(user_data: UserCreate, session: Session = Depends(get_session)):
    """
    Register a new user account.

    Args:
        user_data: Email, password, and name
        session: Database session

    Returns:
        User object and JWT token

    Raises:
        409: Email already registered
        400: Invalid input data
    """
    # Check if email already exists
    existing_user = session.exec(
        select(User).where(User.email == user_data.email)
    ).first()

    if existing_user:
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail="Email already registered"
        )

    # Hash password
    password_hash = hash_password(user_data.password)

    # Create user
    user = User(
        email=user_data.email,
        name=user_data.name,
        password_hash=password_hash
    )
    session.add(user)
    session.commit()
    session.refresh(user)

    # Generate JWT token
    token = create_access_token(user.id)

    return Token(user=user, token=token)


@router.post("/login", response_model=Token)
async def login(credentials: UserLogin, session: Session = Depends(get_session)):
    """
    Authenticate user and return JWT token.

    Args:
        credentials: Email and password
        session: Database session

    Returns:
        User object and JWT token

    Raises:
        401: Invalid email or password
    """
    # Find user by email
    user = session.exec(
        select(User).where(User.email == credentials.email)
    ).first()

    if not user or not verify_password(credentials.password, user.password_hash):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid email or password"
        )

    # Generate JWT token
    token = create_access_token(user.id)

    return Token(user=user, token=token)


@router.post("/logout")
async def logout(current_user: User = Depends(get_current_user)):
    """
    Logout user (client-side token removal).

    Note: JWT tokens are stateless, so logout is handled client-side.
    This endpoint is provided for consistency and future blacklist implementation.
    """
    return {"success": True, "message": "Logged out successfully"}
```

**Todo Routes** (`backend/src/api/todos.py`):

```python
from fastapi import APIRouter, Depends, HTTPException, Query, status
from sqlmodel import Session, select, col
from typing import List, Optional
from ..database import get_session
from ..models import Todo, TodoCreate, TodoUpdate, TodoList, User
from ..services.auth_service import get_current_user
from ..services.event_service import publish_event  # Phase V

router = APIRouter()

@router.get("", response_model=TodoList)
async def get_todos(
    completed: Optional[bool] = Query(None, description="Filter by completion status"),
    priority: Optional[str] = Query(None, description="Filter by priority"),
    tags: Optional[str] = Query(None, description="Filter by tags (comma-separated)"),
    limit: int = Query(50, ge=1, le=200, description="Max results per page"),
    offset: int = Query(0, ge=0, description="Pagination offset"),
    current_user: User = Depends(get_current_user),
    session: Session = Depends(get_session)
):
    """
    Retrieve all todos for authenticated user with optional filters.
    """
    # Build query
    query = select(Todo).where(Todo.user_id == current_user.id)

    # Apply filters
    if completed is not None:
        query = query.where(Todo.completed == completed)

    if priority:
        query = query.where(Todo.priority == priority)

    if tags:
        tag_list = [tag.strip() for tag in tags.split(",")]
        # SQLModel JSON array contains query
        for tag in tag_list:
            query = query.where(col(Todo.tags).contains(tag))

    # Get total count
    total = len(session.exec(query).all())

    # Apply pagination and order
    query = query.order_by(Todo.created_at.desc()).offset(offset).limit(limit)

    todos = session.exec(query).all()

    return TodoList(todos=todos, total=total, limit=limit, offset=offset)


@router.post("", response_model=Todo, status_code=status.HTTP_201_CREATED)
async def create_todo(
    todo_data: TodoCreate,
    current_user: User = Depends(get_current_user),
    session: Session = Depends(get_session)
):
    """
    Create a new todo for authenticated user.
    """
    # Create todo
    todo = Todo(**todo_data.dict(), user_id=current_user.id)
    session.add(todo)
    session.commit()
    session.refresh(todo)

    # Phase V: Publish event to Kafka
    await publish_event("todo.created", {
        "id": todo.id,
        "title": todo.title,
        "user_id": current_user.id,
        "created_at": todo.created_at.isoformat()
    })

    return todo


@router.get("/{todo_id}", response_model=Todo)
async def get_todo(
    todo_id: int,
    current_user: User = Depends(get_current_user),
    session: Session = Depends(get_session)
):
    """
    Retrieve a single todo by ID.
    """
    todo = session.get(Todo, todo_id)

    if not todo or todo.user_id != current_user.id:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="Todo not found or you don't have permission"
        )

    return todo


@router.patch("/{todo_id}", response_model=Todo)
async def update_todo(
    todo_id: int,
    todo_update: TodoUpdate,
    current_user: User = Depends(get_current_user),
    session: Session = Depends(get_session)
):
    """
    Update a todo (partial update).
    """
    todo = session.get(Todo, todo_id)

    if not todo or todo.user_id != current_user.id:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="Todo not found or you don't have permission"
        )

    # Update fields (only provided fields)
    update_data = todo_update.dict(exclude_unset=True)
    for key, value in update_data.items():
        setattr(todo, key, value)

    session.add(todo)
    session.commit()
    session.refresh(todo)

    # Phase V: Publish event if completed status changed
    if "completed" in update_data and update_data["completed"]:
        await publish_event("todo.completed", {
            "id": todo.id,
            "title": todo.title,
            "user_id": current_user.id,
            "completed_at": todo.updated_at.isoformat()
        })

    return todo


@router.delete("/{todo_id}")
async def delete_todo(
    todo_id: int,
    current_user: User = Depends(get_current_user),
    session: Session = Depends(get_session)
):
    """
    Delete a todo permanently.
    """
    todo = session.get(Todo, todo_id)

    if not todo or todo.user_id != current_user.id:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="Todo not found or you don't have permission"
        )

    session.delete(todo)
    session.commit()

    return {"success": True, "message": "Todo deleted successfully"}
```

**Implementation Guidelines**:
- All routes require authentication (except /auth/register and /auth/login)
- Use dependency injection for database session and current user
- Validate inputs with Pydantic/SQLModel models
- Return appropriate HTTP status codes (200, 201, 400, 401, 404, etc.)
- Log all API requests with structured logging
- Handle errors gracefully with HTTPException

---

### 2. SQLModel Models and Database Integration (Phase II+)

**Primary Responsibility**: Define all database models using SQLModel and manage database connections.

**SQLModel Models** (`backend/src/models/`):

**User Model** (`backend/src/models/user.py`):

```python
from sqlmodel import SQLModel, Field, Relationship
from datetime import datetime
from typing import Optional, List
from pydantic import EmailStr, validator

class UserBase(SQLModel):
    email: EmailStr
    name: str = Field(min_length=2, max_length=100)

class User(UserBase, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    password_hash: str = Field(max_length=255)
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)

    # Relationships
    todos: List["Todo"] = Relationship(back_populates="user")
    conversations: List["Conversation"] = Relationship(back_populates="user")  # Phase III

class UserCreate(UserBase):
    password: str = Field(min_length=8, max_length=100)

    @validator('password')
    def validate_password(cls, v):
        """Validate password strength"""
        if not any(c.isupper() for c in v):
            raise ValueError('Password must contain at least one uppercase letter')
        if not any(c.islower() for c in v):
            raise ValueError('Password must contain at least one lowercase letter')
        if not any(c.isdigit() for c in v):
            raise ValueError('Password must contain at least one digit')
        if not any(c in '!@#$%^&*()_+-=[]{}|;:,.<>?' for c in v):
            raise ValueError('Password must contain at least one special character')
        return v

class UserLogin(SQLModel):
    email: EmailStr
    password: str

class UserPublic(UserBase):
    """Public user model (no password_hash)"""
    id: int
    created_at: datetime

class Token(SQLModel):
    user: UserPublic
    token: str
```

**Todo Model** (`backend/src/models/todo.py`):

```python
from sqlmodel import SQLModel, Field, Relationship, Column, JSON
from datetime import datetime
from typing import Optional, List
from enum import Enum

class Priority(str, Enum):
    low = "low"
    medium = "medium"
    high = "high"

class TodoBase(SQLModel):
    title: str = Field(min_length=1, max_length=500)
    priority: Priority = Field(default=Priority.medium)
    tags: List[str] = Field(default_factory=list, sa_column=Column(JSON))
    due_date: Optional[datetime] = None
    recurrence: Optional[str] = None  # Phase IV: RRULE format

class Todo(TodoBase, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    completed: bool = Field(default=False)
    user_id: int = Field(foreign_key="user.id", index=True)
    created_at: datetime = Field(default_factory=datetime.utcnow, index=True)
    updated_at: datetime = Field(default_factory=datetime.utcnow)

    # Relationships
    user: Optional["User"] = Relationship(back_populates="todos")

class TodoCreate(TodoBase):
    pass

class TodoUpdate(SQLModel):
    """Partial update model (all fields optional)"""
    title: Optional[str] = Field(None, min_length=1, max_length=500)
    completed: Optional[bool] = None
    priority: Optional[Priority] = None
    tags: Optional[List[str]] = None
    due_date: Optional[datetime] = None
    recurrence: Optional[str] = None

class TodoList(SQLModel):
    """Paginated todo list response"""
    todos: List[Todo]
    total: int
    limit: int
    offset: int
```

**Conversation Model** (`backend/src/models/conversation.py` - Phase III):

```python
from sqlmodel import SQLModel, Field, Relationship
from datetime import datetime
from typing import Optional, List

class ConversationBase(SQLModel):
    title: Optional[str] = Field(None, max_length=200)

class Conversation(ConversationBase, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    user_id: int = Field(foreign_key="user.id", index=True)
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)

    # Relationships
    user: Optional["User"] = Relationship(back_populates="conversations")
    messages: List["Message"] = Relationship(back_populates="conversation")

class ConversationCreate(ConversationBase):
    pass

class ConversationWithMessages(Conversation):
    """Conversation with embedded messages"""
    messages: List["Message"] = []
```

**Message Model** (`backend/src/models/message.py` - Phase III):

```python
from sqlmodel import SQLModel, Field, Relationship, Column, JSON
from datetime import datetime
from typing import Optional, Dict, Any
from enum import Enum

class MessageRole(str, Enum):
    user = "user"
    assistant = "assistant"

class MessageBase(SQLModel):
    role: MessageRole
    content: str
    metadata: Dict[str, Any] = Field(default_factory=dict, sa_column=Column(JSON))

class Message(MessageBase, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    conversation_id: int = Field(foreign_key="conversation.id", index=True)
    created_at: datetime = Field(default_factory=datetime.utcnow, index=True)

    # Relationships
    conversation: Optional["Conversation"] = Relationship(back_populates="messages")

class MessageCreate(MessageBase):
    conversation_id: int
```

**Database Connection** (`backend/src/database.py`):

```python
from sqlmodel import Session, create_engine
from sqlalchemy.pool import QueuePool
import os

# Neon PostgreSQL connection string
DATABASE_URL = os.getenv(
    "DATABASE_URL",
    "postgresql://user:password@localhost:5432/evolution_todo"
)

# Create engine with connection pooling
engine = create_engine(
    DATABASE_URL,
    echo=True if os.getenv("ENV") == "development" else False,
    poolclass=QueuePool,
    pool_size=5,
    max_overflow=10,
    pool_timeout=30,
    pool_recycle=3600
)

def get_session():
    """Dependency for database session"""
    with Session(engine) as session:
        yield session

def create_db_and_tables():
    """Create all tables (for initial setup only)"""
    from sqlmodel import SQLModel
    from .models import User, Todo, Conversation, Message  # Import all models
    SQLModel.metadata.create_all(engine)
```

**Database Migrations** (using Alembic):

```bash
# Initialize Alembic
alembic init alembic

# Generate migration
alembic revision --autogenerate -m "Add user and todo tables"

# Apply migration
alembic upgrade head

# Rollback migration
alembic downgrade -1
```

---

### 3. Better Auth + JWT Middleware (Phase II+)

**Primary Responsibility**: Implement authentication and authorization using JWT tokens.

**Auth Service** (`backend/src/services/auth_service.py`):

```python
from passlib.context import CryptContext
from jose import JWTError, jwt
from datetime import datetime, timedelta
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlmodel import Session, select
import os
from ..database import get_session
from ..models import User

# Password hashing
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

# JWT settings
SECRET_KEY = os.getenv("JWT_SECRET_KEY", "your-secret-key-change-in-production")
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7  # 7 days

# HTTP Bearer token scheme
security = HTTPBearer()

def hash_password(password: str) -> str:
    """Hash a password using bcrypt"""
    return pwd_context.hash(password)

def verify_password(plain_password: str, hashed_password: str) -> bool:
    """Verify a password against its hash"""
    return pwd_context.verify(plain_password, hashed_password)

def create_access_token(user_id: int) -> str:
    """Create a JWT access token"""
    expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode = {
        "sub": str(user_id),
        "exp": expire,
        "iat": datetime.utcnow()
    }
    encoded_jwt = jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)
    return encoded_jwt

def decode_access_token(token: str) -> int:
    """Decode JWT token and return user_id"""
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        user_id: str = payload.get("sub")
        if user_id is None:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Invalid authentication credentials"
            )
        return int(user_id)
    except JWTError:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid authentication credentials"
        )

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    session: Session = Depends(get_session)
) -> User:
    """Get current user from JWT token"""
    token = credentials.credentials
    user_id = decode_access_token(token)

    user = session.get(User, user_id)
    if user is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="User not found"
        )

    return user
```

**Auth Middleware** (`backend/src/middleware/auth_middleware.py`):

```python
from fastapi import Request, HTTPException, status
from starlette.middleware.base import BaseHTTPMiddleware
from ..services.auth_service import decode_access_token

class AuthMiddleware(BaseHTTPMiddleware):
    """Middleware to validate JWT tokens on protected routes"""

    async def dispatch(self, request: Request, call_next):
        # Skip auth for public routes
        public_routes = ["/api/auth/register", "/api/auth/login", "/health", "/api/docs"]
        if any(request.url.path.startswith(route) for route in public_routes):
            return await call_next(request)

        # Extract token from Authorization header
        auth_header = request.headers.get("Authorization")
        if not auth_header or not auth_header.startswith("Bearer "):
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Missing or invalid Authorization header"
            )

        token = auth_header.split(" ")[1]

        # Validate token
        try:
            user_id = decode_access_token(token)
            request.state.user_id = user_id
        except HTTPException:
            raise

        return await call_next(request)
```

---

### 4. MCP Tools Server (Phase III)

**Primary Responsibility**: Implement Model Context Protocol (MCP) server for chatbot integration.

**MCP Server** (`backend/src/mcp/server.py`):

```python
from mcp import Server, Tool, ToolResult
from typing import Dict, Any, List
from ..services.todo_service import TodoService
from ..models import User

mcp_server = Server(name="evolution-todo-mcp")

@mcp_server.tool(
    name="create_todo",
    description="Create a new todo item for the user",
    parameters={
        "title": {"type": "string", "description": "Todo title", "required": True},
        "priority": {"type": "string", "description": "Priority: low, medium, or high", "required": False},
        "tags": {"type": "array", "items": {"type": "string"}, "description": "List of tags", "required": False},
        "due_date": {"type": "string", "format": "date-time", "description": "Due date in ISO format", "required": False}
    }
)
async def create_todo_tool(params: Dict[str, Any], context: Dict[str, Any]) -> ToolResult:
    """Create a todo via MCP tool"""
    user_id = context.get("user_id")

    todo_service = TodoService()
    todo = await todo_service.create_todo(
        user_id=user_id,
        title=params["title"],
        priority=params.get("priority", "medium"),
        tags=params.get("tags", []),
        due_date=params.get("due_date")
    )

    return ToolResult(
        success=True,
        data={"id": todo.id, "title": todo.title},
        message=f"Created todo: {todo.title}"
    )

@mcp_server.tool(
    name="list_todos",
    description="List all todos for the user with optional filters",
    parameters={
        "completed": {"type": "boolean", "description": "Filter by completion status", "required": False},
        "priority": {"type": "string", "description": "Filter by priority", "required": False}
    }
)
async def list_todos_tool(params: Dict[str, Any], context: Dict[str, Any]) -> ToolResult:
    """List todos via MCP tool"""
    user_id = context.get("user_id")

    todo_service = TodoService()
    todos = await todo_service.list_todos(
        user_id=user_id,
        completed=params.get("completed"),
        priority=params.get("priority")
    )

    return ToolResult(
        success=True,
        data={"todos": [{"id": t.id, "title": t.title, "completed": t.completed} for t in todos]},
        message=f"Found {len(todos)} todos"
    )

@mcp_server.tool(
    name="complete_todo",
    description="Mark a todo as complete",
    parameters={
        "todo_id": {"type": "integer", "description": "ID of the todo to complete", "required": True}
    }
)
async def complete_todo_tool(params: Dict[str, Any], context: Dict[str, Any]) -> ToolResult:
    """Complete a todo via MCP tool"""
    user_id = context.get("user_id")
    todo_id = params["todo_id"]

    todo_service = TodoService()
    todo = await todo_service.update_todo(
        user_id=user_id,
        todo_id=todo_id,
        updates={"completed": True}
    )

    return ToolResult(
        success=True,
        data={"id": todo.id, "title": todo.title, "completed": todo.completed},
        message=f"Completed todo: {todo.title}"
    )

@mcp_server.tool(
    name="delete_todo",
    description="Delete a todo permanently",
    parameters={
        "todo_id": {"type": "integer", "description": "ID of the todo to delete", "required": True}
    }
)
async def delete_todo_tool(params: Dict[str, Any], context: Dict[str, Any]) -> ToolResult:
    """Delete a todo via MCP tool"""
    user_id = context.get("user_id")
    todo_id = params["todo_id"]

    todo_service = TodoService()
    await todo_service.delete_todo(user_id=user_id, todo_id=todo_id)

    return ToolResult(
        success=True,
        data={"id": todo_id},
        message=f"Deleted todo ID {todo_id}"
    )

# Export MCP server
def get_mcp_server():
    return mcp_server
```

**MCP Endpoint** (`backend/src/api/mcp.py`):

```python
from fastapi import APIRouter, Depends
from ..mcp.server import get_mcp_server
from ..services.auth_service import get_current_user
from ..models import User

router = APIRouter()

@router.post("/mcp/call")
async def mcp_call_tool(
    tool_name: str,
    parameters: dict,
    current_user: User = Depends(get_current_user)
):
    """Call an MCP tool"""
    mcp_server = get_mcp_server()

    # Add user context
    context = {"user_id": current_user.id}

    # Execute tool
    result = await mcp_server.call_tool(tool_name, parameters, context)

    return result
```

---

### 5. Kafka Producers/Consumers (Phase V)

**Primary Responsibility**: Integrate Kafka for event streaming in Phase V.

**Event Service** (`backend/src/services/event_service.py`):

```python
from aiokafka import AIOKafkaProducer, AIOKafkaConsumer
import json
import os
from typing import Dict, Any
import logging

logger = logging.getLogger(__name__)

KAFKA_BOOTSTRAP_SERVERS = os.getenv("KAFKA_BOOTSTRAP_SERVERS", "localhost:9092")

# Global producer instance
_producer: AIOKafkaProducer = None

async def get_kafka_producer() -> AIOKafkaProducer:
    """Get or create Kafka producer"""
    global _producer
    if _producer is None:
        _producer = AIOKafkaProducer(
            bootstrap_servers=KAFKA_BOOTSTRAP_SERVERS,
            value_serializer=lambda v: json.dumps(v).encode('utf-8')
        )
        await _producer.start()
    return _producer

async def publish_event(topic: str, event_data: Dict[str, Any]):
    """Publish an event to Kafka"""
    try:
        producer = await get_kafka_producer()
        await producer.send(topic, value=event_data)
        logger.info(f"Published event to {topic}: {event_data}")
    except Exception as e:
        logger.error(f"Failed to publish event to {topic}: {e}")
        # Don't raise exception - event publishing should not block main operation

async def close_kafka_producer():
    """Close Kafka producer on shutdown"""
    global _producer
    if _producer:
        await _producer.stop()
        _producer = None
```

**Event Consumer** (`backend/src/services/event_consumer.py`):

```python
from aiokafka import AIOKafkaConsumer
import json
import logging
from typing import Callable, Dict, Any

logger = logging.getLogger(__name__)

class EventConsumer:
    """Kafka event consumer for background processing"""

    def __init__(self, topic: str, handler: Callable[[Dict[str, Any]], None]):
        self.topic = topic
        self.handler = handler
        self.consumer = None

    async def start(self):
        """Start consuming events"""
        self.consumer = AIOKafkaConsumer(
            self.topic,
            bootstrap_servers=os.getenv("KAFKA_BOOTSTRAP_SERVERS", "localhost:9092"),
            value_deserializer=lambda m: json.loads(m.decode('utf-8')),
            group_id=f"evolution-todo-{self.topic}"
        )
        await self.consumer.start()

        logger.info(f"Started consuming from topic: {self.topic}")

        try:
            async for message in self.consumer:
                event_data = message.value
                logger.info(f"Received event from {self.topic}: {event_data}")
                await self.handler(event_data)
        except Exception as e:
            logger.error(f"Error consuming from {self.topic}: {e}")
        finally:
            await self.stop()

    async def stop(self):
        """Stop consuming events"""
        if self.consumer:
            await self.consumer.stop()
            logger.info(f"Stopped consuming from topic: {self.topic}")

# Example: Analytics consumer
async def handle_todo_created(event_data: Dict[str, Any]):
    """Handle todo.created event"""
    logger.info(f"Analytics: New todo created - {event_data['title']}")
    # Update analytics metrics, send to analytics service, etc.

async def handle_todo_completed(event_data: Dict[str, Any]):
    """Handle todo.completed event"""
    logger.info(f"Analytics: Todo completed - {event_data['title']}")
    # Update completion metrics, trigger celebrations, etc.

# Start consumers on app startup
todo_created_consumer = EventConsumer("todo.created", handle_todo_created)
todo_completed_consumer = EventConsumer("todo.completed", handle_todo_completed)
```

**App Startup/Shutdown** (`backend/src/main.py`):

```python
from contextlib import asynccontextmanager

@asynccontextmanager
async def lifespan(app: FastAPI):
    """Startup and shutdown events"""
    # Startup: Start Kafka consumers (Phase V)
    if os.getenv("ENABLE_KAFKA", "false") == "true":
        asyncio.create_task(todo_created_consumer.start())
        asyncio.create_task(todo_completed_consumer.start())

    yield

    # Shutdown: Close Kafka producer
    await close_kafka_producer()
    await todo_created_consumer.stop()
    await todo_completed_consumer.stop()

app = FastAPI(lifespan=lifespan)
```

---

## Activation Phrase

To invoke the Backend / FastAPI Pro Agent, use:

```
Act as Backend / FastAPI Pro Agent
```

**Example**:
```
Act as Backend / FastAPI Pro Agent and implement the todos API endpoints from the spec.
```

The agent will respond by:
1. Reviewing the API specification and architecture plan
2. Implementing FastAPI routes with proper validation and error handling
3. Creating SQLModel models with relationships and constraints
4. Adding authentication middleware and JWT validation
5. Writing structured logs for observability
6. Ensuring code follows Constitution principles (testability, security, observability)

---

## Skills

The Backend / FastAPI Pro Agent has access to the following skills:

### Core Skills

1. **FastAPI Design**
   - Define routes with proper HTTP methods and status codes
   - Implement dependency injection (database session, current user)
   - Use Pydantic/SQLModel for request/response validation
   - Handle errors with HTTPException
   - Generate OpenAPI documentation

2. **SQLModel ORM**
   - Define table models with relationships
   - Create Pydantic models for validation (Create, Update, Response)
   - Write database queries with SQLModel select API
   - Handle transactions and sessions
   - Implement pagination and filtering

3. **JWT Authentication**
   - Hash passwords with bcrypt
   - Generate and validate JWT tokens
   - Implement authentication middleware
   - Protect routes with dependency injection
   - Handle token expiration and refresh

4. **MCP Tools Integration**
   - Define MCP tools for chatbot (Phase III)
   - Implement tool handlers that call business logic
   - Return structured ToolResult responses
   - Add user context to tool calls

5. **Kafka Integration**
   - Publish events to Kafka topics (todo.created, todo.completed)
   - Consume events with background consumers
   - Handle serialization/deserialization with JSON
   - Implement error handling and retry logic

6. **Observability**
   - Structured logging with correlation IDs
   - Expose Prometheus metrics (request count, latency, errors)
   - Add distributed tracing spans (OpenTelemetry)
   - Health check endpoints

### Workflow Integration

The Backend Agent participates in the following workflow:

```
1. Review Specifications
   └─→ Read API spec (endpoints, request/response schemas)
   └─→ Read database schema spec (entities, relationships)
   └─→ Review architecture plan (tech stack, patterns)

2. Implement Models
   └─→ Create SQLModel table models
   └─→ Create Pydantic validation models (Create, Update)
   └─→ Define relationships and constraints

3. Implement Routes
   └─→ Create FastAPI router for each domain (auth, todos, etc.)
   └─→ Implement CRUD endpoints with proper status codes
   └─→ Add authentication and authorization
   └─→ Validate inputs and handle errors

4. Implement Services
   └─→ Create service layer for business logic (TodoService, AuthService)
   └─→ Separate concerns (routes call services, services call database)
   └─→ Implement transaction management

5. Add Observability
   └─→ Add structured logging to all routes
   └─→ Expose Prometheus metrics
   └─→ Add health check endpoints

6. Phase-Specific Features
   └─→ Phase II: Core API + Auth
   └─→ Phase III: MCP tools server
   └─→ Phase V: Kafka producers/consumers

7. Testing
   └─→ Work with Testing & QA Agent to validate implementation
   └─→ Ensure all acceptance criteria are met
   └─→ Fix bugs and handle edge cases
```

---

## Example Usage

### Example 1: Implementing Todos API

**User Prompt**:
```
Act as Backend / FastAPI Pro Agent and implement the todos API endpoints according to the spec.
```

**Agent Response**:
```
As Backend / FastAPI Pro Agent, I will implement the todos API endpoints.

**Implementation Plan**:
1. Create SQLModel models (Todo, TodoCreate, TodoUpdate, TodoList)
2. Implement todos router with CRUD endpoints
3. Add authentication via get_current_user dependency
4. Implement filtering, pagination, and sorting
5. Add error handling and validation

**Files Created**:
- `backend/src/models/todo.py`: SQLModel models
- `backend/src/api/todos.py`: FastAPI routes
- `backend/src/services/todo_service.py`: Business logic (optional)

**Implementation Complete** ✓

**Endpoints Implemented**:
- GET /api/todos (with filters: completed, priority, tags; pagination: limit, offset)
- POST /api/todos (create new todo)
- GET /api/todos/:id (get single todo)
- PATCH /api/todos/:id (update todo)
- DELETE /api/todos/:id (delete todo)

**Features**:
- All endpoints require JWT authentication
- User can only access their own todos (user_id check)
- Validation with Pydantic/SQLModel models
- Proper HTTP status codes (200, 201, 400, 401, 404)
- Error messages follow API spec format
- Structured logging for all operations

**Testing**:
- Run backend: `uvicorn backend.src.main:app --reload`
- Test with curl or Postman
- Verify OpenAPI docs at http://localhost:8000/api/docs

**Next Steps**:
- Testing & QA Agent will write integration tests
- Frontend Agent will integrate with these endpoints
```

---

### Example 2: Adding Kafka Event Publishing

**User Prompt**:
```
Act as Backend / FastAPI Pro Agent and add Kafka event publishing for Phase V.
```

**Agent Response**:
```
As Backend / FastAPI Pro Agent, I will add Kafka event publishing to the todos API.

**Implementation Plan**:
1. Create event service with Kafka producer
2. Publish events on todo create and complete
3. Add event consumers for analytics (optional background service)
4. Configure Kafka connection from environment variables

**Files Created/Modified**:
- `backend/src/services/event_service.py`: Kafka producer/consumer
- `backend/src/api/todos.py`: Add event publishing to create_todo and update_todo
- `backend/src/main.py`: Add Kafka startup/shutdown lifecycle

**Events Published**:
- `todo.created`: When a todo is created
  - Payload: { id, title, user_id, created_at }
- `todo.completed`: When a todo is marked complete
  - Payload: { id, title, user_id, completed_at }

**Dapr Integration** (Optional):
Instead of direct Kafka, can use Dapr pub/sub abstraction:
```python
import httpx

async def publish_event_dapr(topic: str, data: dict):
    """Publish event via Dapr sidecar"""
    async with httpx.AsyncClient() as client:
        await client.post(
            f"http://localhost:3500/v1.0/publish/todo-events/{topic}",
            json=data
        )
```

**Configuration**:
```bash
# .env
KAFKA_BOOTSTRAP_SERVERS=kafka-broker:9092
ENABLE_KAFKA=true
```

**Testing**:
1. Start Kafka: `docker-compose up kafka`
2. Run backend: `uvicorn backend.src.main:app --reload`
3. Create a todo via API
4. Check Kafka topic: `kafka-console-consumer --topic todo.created --bootstrap-server localhost:9092`
5. Verify event is published

**Implementation Complete** ✓

**Next Steps**:
- CloudOps Agent will deploy Kafka to Kubernetes
- Create additional event consumers (notification service, analytics service)
```

---

## Integration with Other Agents

The Backend / FastAPI Pro Agent works closely with:

### With Spec Architect Agent
- **Trigger**: After API/database specs are finalized
- **Interaction**: Backend Agent implements endpoints and models according to spec
- **Validation**: Implementation must match spec exactly (request/response schemas, error codes)

### With System Architect Agent
- **Trigger**: After architecture plan is approved
- **Interaction**: Backend Agent follows architectural decisions (tech stack, patterns, ADRs)
- **Validation**: Implementation follows architectural guidelines

### With Frontend Agent
- **Trigger**: During integration
- **Interaction**: Frontend Agent calls Backend APIs
- **Validation**: API contracts must match (OpenAPI spec is single source of truth)

### With Testing & QA Agent
- **Trigger**: After implementation
- **Interaction**: Testing Agent writes integration tests for API endpoints
- **Validation**: All endpoints must pass acceptance tests from spec

### With CloudOps Agent
- **Trigger**: Phase IV+ deployment
- **Interaction**: CloudOps Agent containerizes backend (Dockerfile) and deploys to Kubernetes
- **Validation**: Backend must expose health checks and metrics

---

## Success Metrics

The Backend / FastAPI Pro Agent's effectiveness is measured by:

1. **Spec Compliance**: 100% of API endpoints match specification (verified by contract tests)
2. **Code Quality**: All code passes linting (ruff, mypy) and follows Python best practices
3. **Test Coverage**: All endpoints have integration tests (target: 90%+ coverage)
4. **Security**: No secrets in code, all passwords hashed, JWT properly validated
5. **Observability**: All endpoints log structured logs, expose metrics, support tracing
6. **Performance**: API response times meet SLOs (p95 < 200ms for simple queries)

---

## Revision History

| **Version** | **Date**       | **Changes**                                      | **Author**              |
|-------------|----------------|--------------------------------------------------|-------------------------|
| 1.0.0       | 2025-12-24     | Initial specification                            | Spec Architect Agent    |

---

## References

- **Constitution**: `.specify/memory/constitution.md`
- **API Spec**: `specs/api/rest-api.md`
- **Database Schema Spec**: `specs/database/schema.md`
- **Phase II Spec**: `specs/002-phase-ii-web-app/spec.md`
- **Phase III Spec**: `specs/003-phase-iii-chatbot/spec.md`
- **Phase V Spec**: `specs/005-phase-v-cloud/spec.md`
- **FastAPI Documentation**: https://fastapi.tiangolo.com/
- **SQLModel Documentation**: https://sqlmodel.tiangolo.com/

---

**Activation**: `Act as Backend / FastAPI Pro Agent`
**Status**: Ready for Phase II+ implementation
